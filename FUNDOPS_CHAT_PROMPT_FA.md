# پرامپت آماده پیست — برای چت مخصوص پروژه FundOps

> این متن را کامل در چت پروژه `fintech-fund-` پیست کن.

---

یک ممیزی بیرونی (۲۶ جولای) روی HEAD = `47b913f` انجام شده و وضعیت فعلی راستی‌آزمایی شده. زمینه: VC گفته «به $60k برس، سرمایه می‌دهیم» و سند معیار GTM ما `docs/STRATEGY-FAST-REVENUE.md` است (قانون کل سند: **یک گُوِه، دو جغرافیا، یک CTA**). فاندر به‌موازات تو «اسپرینت دوهفته‌ای سیگنال» (`sales/two-week-sprint.md`) را اجرا می‌کند. کار تو: اجرای صف ساخت §۳ همان سند استراتژی، به ترتیب، بدون انحراف.

## وضعیت تأییدشده (دوباره‌کاری نکن)

- ✅ آیتم ۱ صف ساخت انجام شده: `/sandbox` (stateless، با تست — `src/sandbox/`) و `/pricing` موجودند؛ ۵۳ تست سبز؛ لندینگ `site/index.html` ساخته شده؛ فایل‌های دیپلوی HF در `deploy/huggingface/` (Dockerfile + README) آماده‌اند.
- ❌ آیتم‌های ۲ تا ۷ هنوز ساخته نشده‌اند (در کد هیچ ردی از register-diff، order form/لینک پرداخت، close pack، قالب‌های عربی RTL، ران‌بوک provisioning، magic-link نیست).

## صف ساخت — به همین ترتیب اجرا کن (از §۳ استراتژی)

1. **موتور register-diff (~۲ روز):** diff شبانه DFSA + FSRA + FCA Register API + LPهای جدید Companies House + واچ‌لیست ۱۰ صندوق BBB (`sales/targets.md` A1) → خروجی: لیست روزانه لیدِ در-تریگر با «خط اول» آماده برای پیام. خروجی باید مستقیم قابل‌مصرف در ریتم روزانه ۹۰ دقیقه‌ای فاندر باشد.
2. **لینک پرداخت + فرم سفارش ۲ صفحه‌ای (~۱ روز):** سه پیشنهاد — پک $3,500 (۱۰۰٪ پیش، تا ۶۰ روز credit روی اشتراک) / فصلی $4,500 / سالانه $13k — با بند walk-away چاپ‌شده. هدف: حذف زمان مرده «بله → پول».
3. **پک close حسابرس‌پسند (~۲ روز):** زیپ یک‌کلیکی: CSV آدیت + جدول تخصیص + صورت‌حساب‌ها + PDFها + کاور «deterministic — no LLM touches money».
4. **قالب‌های عربی RTL (~۳ روز):** اطلاعیه + گزارش روی همان پایپ‌لاین PDF؛ QA زبانی با فاندر. (توجیه تیر $15k GCC Premium.)
5. **ران‌بوک provisioning هر-مشتری + تم white-label (~۳ روز):** اسپین‌آپ ۳۰ دقیقه‌ای کانتینر ایزوله به‌ازای هر مشتری + لوگوی مشتری روی اسناد.
6. **magic-link به کانتینر خود مشتری (~۴ روز):** **عمداً آخر** — فقط بعد از فروش ۲ پک اول شروع کن.

## قوانین دائمی (از استراتژی + `sales/plan-60k.md` — نقض نکن)

- ❌ هیچ کار UI روی waterfall/IRR («قول ندیده نده» — در دمو هم اسکریپت همین را می‌گوید).
- ❌ هیچ auth کامل multi-tenant الان؛ مدل، کانتینر ایزوله per-customer + magic-link (آیتم ۶) است.
- ❌ هیچ لید US، هیچ white-label به adminها (ماه ۴–۹).
- 🔐 تا رمزنگاری at-rest ساخته نشده، **هیچ سند هویتی واقعی LP وارد vault نشود** — سیاست pilot: فقط داده عملیاتی + bank ref ماسک‌شده. اگر خواستی رمزنگاری را جلو بیندازی، بعد از آیتم ۳ صف، نه قبلش.
- ✅ هر آیتم: تست + `npm test` و `npm run build` سبز + آپدیت docs مربوطه در همان کامیت.

## سه کار کوچک اضافه (کم‌هزینه، در حاشیه صف)

- سلامت `deploy/huggingface/` را با app فعلی دوبار چک کن (پورت 7860/non-root طبق `docs/DEPLOY.md` A0) تا دیپلوی اول فاندر بدون خطا دربیاید؛ اگر چیزی drift کرده fix کن.
- لینک sandbox و بوکینگ، وقتی فاندر داد، در `site/index.html` و متریال‌های `sales/` جایگذاری شود (الان placeholder اند).
- بعد از هر آیتم تمام‌شده، `docs/BUILD-PLAN.md` (یا STATE مربوطه) را با وضعیت واقعی sync کن.

## فرمت گزارش بعد از هر آیتم

«آیتم N تمام: چه ساخته شد · کجا (مسیر فایل‌ها) · چطور تست شد · فاندر الان دقیقاً چه کاری با آن می‌تواند بکند · آیتم بعدی صف.»

اول از همه: وضعیت فعلی را خودت verify کن (تست‌ها، build)، بعد آیتم ۱ صف (register-diff) را شروع کن.
